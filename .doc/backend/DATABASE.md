# 数据库与存储设计（边界版）

## 1. 现状说明
前端仓库不直接访问数据库，不掌握后端真实存储结构。

## 2. 可确认事实
1. 会话、运行、事件等数据由上游服务持有并通过 API 返回。
2. 前端仅消费响应结果，不定义持久化 schema。

## 3. `[DOC-GAP]` 存储细节缺失
- 影响路径：`.doc/backend/DATABASE.md`
- 缺口：数据库类型、主键策略、索引策略未知。
- 风险：若臆测会误导调试与排障。
- 默认策略：仅维护 API 可观测事实，不编造存储层细节。
