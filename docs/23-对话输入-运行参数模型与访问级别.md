# 运行参数模型与访问级别

## 当前状态
Composer 的运行参数包含 access level、模型覆盖、reasoning effort、service tier、planning/editing mode 和单次 must-use skills。通用控件由 `QuerySettingsControls` 承载，Mode 从 Composer “+”菜单选择，Skills 从 slash palette 选择。

## 核心职责
- 展示并持久化当前对话或当前 agent 的模型选择。
- native 模型的 reasoning effort 统一为 `NONE / LOW / MEDIUM / HIGH / XHIGH / MAX`，默认 `MEDIUM`。
- 为允许模型选择的 Agent 加载模型选项并处理默认模型覆盖。
- Composer 模型子菜单的模型列表表头提供手动刷新入口，强制重新请求 `/api/model-options` 并忽略 Agent 内嵌 `modelOptions` 与前端缓存；刷新失败保留原列表并在模型按钮旁提示。
- 将 access level、model override 和控制参数随 query payload 发送。
- Mode 按智能体类型显示：`CODER` 提供 Planning，`KBASE` 提供 Editing，普通 Agent 与 Team 不显示 Mode 分组。
- Slash Skills 通过 `/api/skills` 展示当前 Agent 已配置 Skill 与有效技能市场 Skill，支持单条消息多选；Team 不加载或展示该分组。
- `mustUseSkills` 进入 `/api/query` 的 SSE/WebSocket payload；发送进入 query 链路后清理，切换智能体时同步清理，旧 `requiredSkillKeys` 不再发送。
- 在运行中支持访问级别调整，并同步前端 agents 状态。

## 核心流程
选中 Agent 后加载详情及解析后的 interactionConfig；详情未就绪时关闭对应入口。Composer 打开运行参数控件时加载模型选项；输入独立 slash token 时按当前 Agent 加载 `/api/skills`，Platform 通过同名 WS request 严格请求，Gateway 因能力表未开放该 route 而静态使用 HTTP，WS 传输故障不回退。选中 Skill 后清除 slash 查询并生成可逐项移除的“必须使用”标签。发送消息时，`buildControlsParams`、`modelOverride`、`accessLevel`、mode 和 `mustUseSkills` 被传入发送事件，最终由 `buildQueryPayload` 写入 `/api/query` 请求。reasoning effort 在 Composer 自定义事件、BTW、HTTP/SSE 与 WebSocket payload 中统一归一为大写逻辑值；兼容输入 `EXTRA_HIGH` 归一为 `XHIGH`，前端不执行 provider 模型翻译。若 @mention 或最终路由智能体与选择来源不一致，前端阻止发送。运行中调整 access level 时调用 data client，并用当前 `RunOwner` 序列化身份：Agent 发送 `agentKey`，编排 Team 仅发送 `teamId`。

## 边界与非目标
- 前端只展示后端返回的模型与权限能力，不自行推断真实权限。
- 模型、权限级别、必用技能、连接器、本地文件和聊天记录入口消费 `/api/agent.interactionConfig`。REACT 默认全开，CODER 默认关闭聊天记录，KBASE 默认关闭模型、权限级别、连接器和聊天记录。配置缺省按后端 mode 默认值解析；无网盘文件字段。
- must-use skills 是单次、多选约束；Agent 已配置与技能市场项都可选，最终可用性由 Platform 校验，Team 不提供选择入口。
- 模型参数只作为请求提示，最终执行语义由后端决定。

## 相关文件
- `../src/features/composer/components/QuerySettingsControls.tsx`
- `../src/features/composer/components/SlashPalette.tsx`
- `../src/features/composer/hooks/useComposerSend.ts`
- `../src/features/composer/hooks/useRuntimeAccessLevel.ts`
- `../src/features/composer/lib/queryRouting.ts`
- `../src/shared/data/api/client.ts`
- `../src/shared/data/api/endpoints.ts`
- `../src/app/state/navigationTypes.ts`

Agent 编辑页提供六个对话输入开关及恢复类型默认值操作。模型关闭时使用已配置模型；权限级别关闭时使用 default，工具审批继续生效。必用技能关闭不影响 Agent 已配置技能。附件开关覆盖文件选择、拖放、粘贴、截图和聊天记录引用。


### 模型可选项与已选值

`/api/model-options` 只提供 `models`、`reasoningEfforts`、可选 `serviceTiers`，不返回选择或 default 字段。`/api/agent` 使用顶层 `modelKey/reasoningEffort/serviceTier`；NONE 表示关闭思考，未配置服务等级省略字段。详情不再返回 `model`、`selected*`、`modelConfig`、`modelOptions` 或 meta 中重复的模型信息，ACP 详情不请求上游能力、不自动替换配置的 modelKey。模型暂时不可用时前端保留配置并提示，不自动选其他模型。

`POST /api/agent/model-config` 只接受 `agentKey`（必填），以及至少一个 `modelKey/reasoningEffort/serviceTier`；省略设置保持原值，`serviceTier:null` 清除等级。响应为扁平 `{agentKey,modelKey,reasoningEffort,serviceTier?}`。不接受 key 别名和未知字段。选项缓存和手动刷新只更新能力列表。模型选择在保存成功后更新 Agent 状态。

`/api/agent.skills` 为 `{key,name}[]`，无技能时为空数组；管理编辑器转换为 key 数组写入 `definition.skillConfig.skills`，内部 YAML 结构不变。`meta.perAgentSkills` 已移除。
